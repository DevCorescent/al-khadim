import * as documentRequests from '@/server/controllers/documentRequests.controller';

export const dynamic = 'force-dynamic';

export const GET = documentRequests.get;
export const DELETE = documentRequests.remove;
